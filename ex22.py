# 自己整理笔记
int = 1 # 整数类型
float = 3.14 # 浮点数，即小数
bool = False
bool = True # 布尔数 即 真假
print(True + 1) # 布尔值 Fales = 0， Ture = 1,所以这里print结果等于2
str # 字符串,即我们常见的文字
/ # /转意符，/n 等于新起一行，// 把/以字符串形式显示 等等
from bs4 import BeautifulSoup # 从 xx 导入 xx 模块，模块即别人写好的某种功能函数库
argv 通过传参函数
def # 定义函数 def (name):
# 到此为止